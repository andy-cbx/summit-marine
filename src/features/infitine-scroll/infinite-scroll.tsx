import * as React from 'react';type InfiniteScrollProps = {    children: React.ReactNode;};export const InfiniteScroll: React.FC<InfiniteScrollProps> = ({ children }) => {    const [looperInstances, setLooperInstances] = React.useState(2);    const outerRef: React.MutableRefObject<HTMLDivElement | null> =        React.useRef(null);    const innerRef: React.MutableRefObject<HTMLDivElement | null> =        React.useRef(null);    function resetAnimation() {        if (innerRef?.current) {            innerRef.current.setAttribute('data-animate', 'false');            setTimeout(() => {                if (innerRef?.current) {                    innerRef.current.setAttribute('data-animate', 'true');                }            }, 50);        }    }    React.useEffect(() => {        resetAnimation();    }, []);    return (        <div            ref={outerRef}            className={`w-full max-w-[unset] overflow-hidden`}        >            <div                className={`box-border flex w-max max-w-[unset] animate-none justify-center overflow-x-hidden [&[data-animate=true]]:animate-infinite-scroll-horizontal`}                ref={innerRef}            >                {[...Array(looperInstances)].map((_, ind) => (                    <div key={ind}>                        {children}                    </div>                ))}            </div>        </div>    );};