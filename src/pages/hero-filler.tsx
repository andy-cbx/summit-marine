import * as React from 'react';import { useRef } from 'react';import { cn } from '@/shared/lib/cn';export type HeroFillerProps = {	img: string;	text?: string;	className?: string;	imgClassName?: string;};export const HeroFiller: React.FC<HeroFillerProps> = ({	img,	text,	className,	imgClassName,}) => {	const imgRef = useRef<HTMLImageElement>(null);	return (		<section>			<div				className={cn(					'relative h-[50vh] w-full origin-bottom overflow-hidden shadow-2xl',					className,				)}			>				<img					ref={imgRef}					src={img}					alt={''}					className={cn(						'size-full origin-bottom object-cover object-center brightness-80',						imgClassName,					)}				/>				<div					className={						'absolute top-0 flex h-[50vh] w-full items-center justify-center px-4 des:px-0'					}				>					{text && (						<h1							className={cn(								'relative text-center font-primary font-bold tracking-wide text-white',								'text-4xl tab:text-7xl des:text-7xl',							)}							style={{								textShadow:									'0px 1px 1px rgb(0 0 0 / 0.1), 0px 1px 2px rgb(0 0 0 / 0.1), 0px 2px 4px rgb(0 0 0 / 0.1)',							}}						>							{text}						</h1>					)}				</div>			</div>		</section>	);};