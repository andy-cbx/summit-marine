import { motion } from 'framer-motion';import * as React from 'react';import logo from '@/assets/Summit+marine+mobile+logo.png';import heroVideo from '@/assets/video/AdobeStock_990397910.mov';import hero from '@/assets/View+of+the+blue+ocean+from+boat+front.jpg';import { cn } from '@/utils';export const Landing = () => {	return (		<section			className={				'relative flex h-screen w-screen justify-center overflow-hidden'			}		>			{/*<img*/}			{/*	src={hero}*/}			{/*	alt={'hero'}*/}			{/*	className={'w-full origin-bottom object-cover object-bottom'}*/}			{/*/>*/}			<video				src={heroVideo}				autoPlay				loop				controls={false}				muted				className={'size-full object-cover'}			/>			<motion.div				initial={{ opacity: 0, y: 10, filter: 'blur(10px)' }}				animate={{ opacity: 1, y: 0, filter: 'blur(0px)' }}				transition={{ delay: 0.5, duration: 1 }}				className={					'absolute flex h-full w-content flex-col items-center justify-center'				}			>				<img src={logo} alt={'logo'} className={''} />				<div className={'flex-col items-center justify-center'}>					<p						className={							'text-center font-primary text-4xl tracking-widest text-white'						}					>						Servicing Sydney for over 30 years					</p>					<div						className={							'flex flex-row items-center justify-center gap-4 pt-8'						}					>						<button							className={cn(								'w-64 rounded-2xl border-2 border-white py-4 font-primary text-2xl text-white transition-all duration-500',								'hover:bg-white hover:text-[#a8a6a1]',							)}						>							Our Services						</button>						<button							className={cn(								'w-64 rounded-2xl bg-[#a8a6a1] py-4 font-primary text-2xl text-white transition-all duration-500 border-2 border-white',								'hover:bg-white hover:text-[#a8a6a1]',							)}						>							Let&#39;s Chat						</button>					</div>				</div>			</motion.div>		</section>	);};