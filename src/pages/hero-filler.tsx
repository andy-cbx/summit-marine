import * as React from 'react';import { useEffect, useRef, useState } from 'react';import { cn } from '@/shared/lib/cn';export type HeroFillerProps = {	img: string;	text?: string;	className?: string;	imgClassName?: string;	objPosition?: string;	isZoom?: boolean;};export const HeroFiller: React.FC<HeroFillerProps> = ({	img,	text,	className,	imgClassName,	objPosition,	isZoom = false,}) => {	const imgRef = useRef<HTMLImageElement>(null);	// const { scrollYProgress } = useScroll({ target: imgRef });	// const scale = useTransform(scrollYProgress, [1, 0], [1.1, 1]);	// const blurMv = useTransform(	// 	scrollYProgress,	// 	[1, 0.6],	// 	['blur(10px)', 'blur(0px)'],	// );	//	// useMotionValueEvent(scrollYProgress, 'change', (val) => {	// 	console.log(val);	// });	return (		<section>			<div				className={cn(					'relative h-[50vh] w-full origin-bottom overflow-hidden shadow-2xl',					className,				)}			>				<img					// style={{					// 	filter: blurMv,					// 	objectPosition: objPosition ? objPosition : undefined,					// 	transformOrigin: objPosition ? objPosition : undefined,					// 	scale: isZoom ? scale : 1,					// }}					ref={imgRef}					src={img}					alt={''}					className={cn(						'size-full origin-bottom object-cover object-center brightness-80',						imgClassName,					)}				/>				<div					className={						'absolute top-0 flex h-[50vh] w-full items-center justify-center'					}				>					{text && (						<h1							className={								'relative hd:p-16 text-center font-primary text-7xl hd:text-7xl font-bold tracking-wide text-white'								// cn(								// 	'relative p-16 font-primary text-7xl font-bold text-transparent bg-clip-text bg-hero bg-cover bg-center bg-no-repeat',								// 	'before:absolute before:content-[""] before:z-[-2] before:inset-0 before:bg-inherit',								// 	'after:absolute after:content-[""] after:inset-10 after:inline-block after:bg-white  after-z[-1]',								// )							}							style={{								textShadow:									'0px 1px 1px rgb(0 0 0 / 0.1), 0px 1px 2px rgb(0 0 0 / 0.1), 0px 2px 4px rgb(0 0 0 / 0.1)',							}}						>							{text}						</h1>					)}				</div>			</div>		</section>	);};