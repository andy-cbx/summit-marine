import React, { useEffect, useRef, useState } from 'react';import {	MdOutlinePhoneIphone,	MdOutlineEmail,	MdOutlineLocationOn,} from 'react-icons/md';import portrait from '@/shared/assets/portrait-placeholder.webp';import { MapView } from '@/features/map/map';// const contentVariants = {// 	initial: {// 		y: 20,// 		opacity: 0.0,// 		filter: 'blur(10px)',// 		transition: { duration: 0.5 },// 	},// 	inView: {// 		opacity: 1.0,// 		y: '0px',// 		filter: 'blur(0px)',// 		transition: { duration: 0.5 },// 	},// };export const Chat = () => {	const imgRef = useRef<HTMLImageElement>(null);	// const { scrollYProgress } = useScroll({ target: imgRef });	// const filterBlur = useTransform(scrollYProgress, [1, 0.8], ['5px', '0px']);	// const [blur, setBlur] = useState('0px');	//	// useEffect(() => {	// 	const removeB = filterBlur.on('change', setBlur);	//	// 	return () => {	// 		removeB();	// 	};	// }, []);	return (		<section			className={				'flex w-full flex-col items-center justify-center bg-black/10 py-32'			}		>			<div				className={					'flex w-content flex-row  backdrop-blur-md bg-blue-300/5 shadow-2xl rounded-3xl'				}			>				<div					className={						'flex w-full flex-1 flex-col justify-center gap-8 pt-16 '					}				>					<h1						// initial={'initial'}						// whileInView={'inView'}						// variants={contentVariants}						// viewport={{ once: false, amount: 'all' }}						className={							'font-primary text-5xl font-bold uppercase tracking-wide text-white/80 text-center'						}						style={{							textShadow:								'0px 1px 1px rgb(0 0 0 / 0.1), 0px 1px 2px rgb(0 0 0 / 0.1), 0px 2px 4px rgb(0 0 0 / 0.1)',						}}					>						CONTACT US					</h1>					<p						// initial={'initial'}						// whileInView={'inView'}						// variants={contentVariants}						// viewport={{ once: false, amount: 'all' }}						className={							'font-primary text-xl font-bold tracking-wide text-white/80 px-16'						}						style={{							textShadow:								'0px 1px 1px rgb(0 0 0 / 0.1), 0px 1px 2px rgb(0 0 0 / 0.1), 0px 2px 4px rgb(0 0 0 / 0.1)',						}}					>						Chat to James Walker – Sydney’s trusted marine						refrigeration and air conditioning specialist.					</p>					<div className={'flex flex-col gap-4 px-24'}>						<span							// initial={'initial'}							// whileInView={'inView'}							// variants={contentVariants}							// viewport={{ once: false, amount: 'all' }}							className={								'flex items-center gap-3 text-justify font-primary text-lg text-black/70'							}						>							<MdOutlineEmail size={28} />							service@smrsyd.com						</span>						<span							// initial={'initial'}							// whileInView={'inView'}							// variants={contentVariants}							// viewport={{ once: false, amount: 'all' }}							className={								'flex items-center gap-3 text-justify font-primary text-lg text-black/70'							}						>							<MdOutlinePhoneIphone size={28} />							0418 260 548						</span>						<span							// initial={'initial'}							// whileInView={'inView'}							// variants={contentVariants}							// viewport={{ once: false, amount: 'all' }}							className={								'flex items-center gap-3 text-justify font-primary text-lg text-black/70'							}						>							<MdOutlineLocationOn size={28} />							10 Winbourne Road Brookvale, NSW 2100						</span>					</div>					<div className={'h-full pt-16'}></div>				</div>				<div					// initial={'initial'}					// whileInView={'inView'}					// variants={contentVariants}					// viewport={{ once: false, amount: 'all' }}					className={						'relative flex aspect-[2/1] w-full flex-1 justify-end overflow-hidden rounded-e-2xl shadow-2xl'					}				>					<MapView />				</div>			</div>		</section>	);};