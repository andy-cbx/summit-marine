import { useGSAP } from '@gsap/react';import React from 'react';import { cn } from '@/shared/lib/cn';import ScrollSmoother from 'gsap/ScrollSmoother';import { useScrollContext } from '@/widgets/layout/context.scroll-container';export const ScrollContainer = React.memo(	({ children }: { children: React.ReactNode }) => {		const { scrollRef } = useScrollContext();		useGSAP(() => {			if (scrollRef) {				scrollRef.current = ScrollSmoother.create({					wrapper: '#main-wrapper',					content: '#main-content',					smooth: 1.3,					effects: true,				});			}		}, [scrollRef]);		return (			<div id={'main-wrapper'} className={cn('w-full')}>				<div id={'main-content'} className={'relative w-full'}>					{children}				</div>			</div>		);	},);ScrollContainer.displayName = 'ScrollContainer';