import { motion, useScroll, useTransform } from 'framer-motion';import React, { useEffect, useRef, useState } from 'react';import {	MdOutlinePhoneIphone,	MdOutlineEmail,	MdOutlineLocationOn,} from 'react-icons/md';import portrait from '@/assets/portrait-placeholder.webp';import { MapView } from '@/features/map/map';const contentVariants = {	initial: {		y: 20,		opacity: 0.0,		filter: 'blur(10px)',		transition: { duration: 0.5 },	},	inView: {		opacity: 1.0,		y: '0px',		filter: 'blur(0px)',		transition: { duration: 0.5 },	},};export const Chat = () => {	const imgRef = useRef<HTMLImageElement>(null);	const { scrollYProgress } = useScroll({ target: imgRef });	const filterBlur = useTransform(scrollYProgress, [1, 0.8], ['5px', '0px']);	const [blur, setBlur] = useState('0px');	useEffect(() => {		const removeB = filterBlur.on('change', setBlur);		return () => {			removeB();		};	}, []);	return (		<section			className={				'flex w-full flex-col items-center justify-center bg-black/10 py-32'			}		>			<div className={'flex w-content flex-row gap-8'}>				<motion.div					className={						'flex w-full flex-1 flex-col justify-center gap-8 '					}				>					<motion.h1						initial={'initial'}						whileInView={'inView'}						variants={contentVariants}						viewport={{ once: false, amount: 'all' }}						className={							'font-primary text-5xl font-bold uppercase tracking-wide text-primary-grey'						}					>						CONTACT US					</motion.h1>					<motion.p						initial={'initial'}						whileInView={'inView'}						variants={contentVariants}						viewport={{ once: false, amount: 'all' }}						className={							'pr-32 font-primary text-xl font-bold tracking-wide text-black/70'						}					>						Chat to James Walker – Sydney’s trusted marine						refrigeration and air conditioning specialist.					</motion.p>					<div className={'flex flex-col gap-4'}>						<motion.span							initial={'initial'}							whileInView={'inView'}							variants={contentVariants}							viewport={{ once: false, amount: 'all' }}							className={								'flex items-center gap-3 text-justify font-primary text-lg text-black/70'							}						>							<MdOutlineEmail size={28} />							service@smrsyd.com						</motion.span>						<motion.span							initial={'initial'}							whileInView={'inView'}							variants={contentVariants}							viewport={{ once: false, amount: 'all' }}							className={								'flex items-center gap-3 text-justify font-primary text-lg text-black/70'							}						>							<MdOutlinePhoneIphone size={28} />							0418 260 548						</motion.span>						<motion.span							initial={'initial'}							whileInView={'inView'}							variants={contentVariants}							viewport={{ once: false, amount: 'all' }}							className={								'flex items-center gap-3 text-justify font-primary text-lg text-black/70'							}						>							<MdOutlineLocationOn size={28} />							10 Winbourne Road Brookvale, NSW 2100						</motion.span>					</div>					<div className={'h-full pt-16'}></div>				</motion.div>				<motion.div					initial={'initial'}					whileInView={'inView'}					variants={contentVariants}					viewport={{ once: false, amount: 'all' }}					className={						'relative flex aspect-[2/1] w-full flex-1 justify-end overflow-hidden rounded-2xl shadow-2xl'					}				>					<MapView />				</motion.div>			</div>		</section>	);};